export const selectTextSearch = (state) => state.filter.textSearch;
export const selectStatusSelected = (state) => state.filter.statusSelected;
export const selectPrioritiesSelected = (state) => state.filter.prioritiesSelected;